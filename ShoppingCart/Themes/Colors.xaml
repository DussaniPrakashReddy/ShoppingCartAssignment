<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ========================================================= -->
    <!-- BRAND COLORS                                               -->
    <!-- ========================================================= -->

    <Color x:Key="PrimaryColor">#2563EB</Color>
    <Color x:Key="PrimaryDarkColor">#1D4ED8</Color>


    <!-- ========================================================= -->
    <!-- APPLICATION COLORS                                         -->
    <!-- ========================================================= -->

    <Color x:Key="BackgroundColor">#F5F7FA</Color>
    <Color x:Key="SurfaceColor">#FFFFFF</Color>


    <!-- ========================================================= -->
    <!-- TEXT COLORS                                                -->
    <!-- ========================================================= -->

    <Color x:Key="TextPrimaryColor">#172033</Color>
    <Color x:Key="TextSecondaryColor">#667085</Color>
    <Color x:Key="TextMutedColor">#98A2B3</Color>


    <!-- ========================================================= -->
    <!-- BORDER COLORS                                              -->
    <!-- ========================================================= -->

    <Color x:Key="BorderColor">#E4E7EC</Color>


    <!-- ========================================================= -->
    <!-- STATUS COLORS                                              -->
    <!-- ========================================================= -->

    <Color x:Key="SuccessColor">#16A34A</Color>
    <Color x:Key="DangerColor">#DC3545</Color>
    <Color x:Key="WarningColor">#F59E0B</Color>


    <!-- ========================================================= -->
    <!-- SIDEBAR COLORS                                             -->
    <!-- ========================================================= -->

    <Color x:Key="SidebarColor">#172033</Color>
    <Color x:Key="SidebarSelectedColor">#2B3B59</Color>


    <!-- ========================================================= -->
    <!-- BRUSHES                                                    -->
    <!-- ========================================================= -->

    <SolidColorBrush x:Key="PrimaryBrush"
                     Color="{StaticResource PrimaryColor}" />

    <SolidColorBrush x:Key="PrimaryDarkBrush"
                     Color="{StaticResource PrimaryDarkColor}" />

    <SolidColorBrush x:Key="BackgroundBrush"
                     Color="{StaticResource BackgroundColor}" />

    <SolidColorBrush x:Key="SurfaceBrush"
                     Color="{StaticResource SurfaceColor}" />

    <SolidColorBrush x:Key="TextPrimaryBrush"
                     Color="{StaticResource TextPrimaryColor}" />

    <SolidColorBrush x:Key="TextSecondaryBrush"
                     Color="{StaticResource TextSecondaryColor}" />

    <SolidColorBrush x:Key="TextMutedBrush"
                     Color="{StaticResource TextMutedColor}" />

    <SolidColorBrush x:Key="BorderBrush"
                     Color="{StaticResource BorderColor}" />

    <SolidColorBrush x:Key="SuccessBrush"
                     Color="{StaticResource SuccessColor}" />

    <SolidColorBrush x:Key="DangerBrush"
                     Color="{StaticResource DangerColor}" />

    <SolidColorBrush x:Key="WarningBrush"
                     Color="{StaticResource WarningColor}" />

    <SolidColorBrush x:Key="SidebarBrush"
                     Color="{StaticResource SidebarColor}" />

    <SolidColorBrush x:Key="SidebarSelectedBrush"
                     Color="{StaticResource SidebarSelectedColor}" />

</ResourceDictionary>